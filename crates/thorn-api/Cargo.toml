[package]
name = "thorn-api"
description = "Stable plugin API for the Thorn Python linter"
version.workspace = true
edition.workspace = true
license.workspace = true
repository = "https://github.com/pescheckit/thorn"

[dependencies]
serde = { workspace = true }
